name: Firebase Continous Deployment

on:
  push: 
    branches: [ master ]

jobs: 
  deploy: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@aster
        with:
          node-version: 12
      - run: npm install
      - run: npm generate
